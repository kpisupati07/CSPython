password = ""

upper = False
lower = False
digit = False
password = input('Whats the password')
skip
while len(password) < 8 or not upper pr not digit:
    if skip != 0:
          print('invalid password')

for character in password:
        if i.isupper():
            upper = True
        elif character.islower():
              lower = true
        elif character.isnumeric():
              digit = true

counter += 1


